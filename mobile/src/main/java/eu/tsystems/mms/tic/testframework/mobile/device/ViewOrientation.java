package eu.tsystems.mms.tic.testframework.mobile.device;

/**
 * Describes the Orientation of the Display of a Smartphone.
 * When turned around 90° on the axis from your face through the phone, the orientation will change.
 * Standard is Portrait.
 *
 * Created by rnhb on 17.04.2015.
 */
public enum ViewOrientation {
    /**
     * Mode that has an aspect ration like a typical portrait (higher than wider).
     */
    PORTRAIT,
    /**
     * Mode that has an aspect ratio, like a typical landscape drawing (wider than higher).
     */
    LANDSCAPE
}
