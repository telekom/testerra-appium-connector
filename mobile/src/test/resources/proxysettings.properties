# You can define every java network setting property here. Have a look in the docs:
# https://docs.oracle.com/javase/8/docs/api/java/net/doc-files/net-properties.html

##################### http settings
http.proxyHost=proxy.mms-dresden.de
http.proxyPort=8080
http.proxyUser=
http.proxyPassword=

# Indicates the hosts that should be accessed without going through the proxy.
# The value of this property is a list of hosts, separated by the '|' character.
# In addition the wildcard character '*' can be used for pattern matching.
# (default: localhost|127.*|[::1])
http.nonProxyHosts=192.168.*

##################### https settings
https.proxyHost=proxy.mms-dresden.de
https.proxyPort=8080
https.proxyUser=
https.proxyPassword=